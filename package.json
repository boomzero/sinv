{
  "name": "sinv",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "node scripts/test-frame-performance.mjs && node scripts/test-world.mjs && node scripts/test-escape.mjs && node scripts/flight-benchmark.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@dimforge/rapier2d-compat": "^0.14.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^8.0.16"
  }
}
